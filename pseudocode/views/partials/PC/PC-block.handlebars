<li>
    {{name}}

    <button class="change-completed" data-id="{{id}}" data-newcompleted="{{completed}}">
        {{#if completed}}mark as completed{{else}}WAKE UP!{{/if}}
    </button>

    <button class="delete-code" data-id="{{this.id}}">DELETE!</button>
</li>